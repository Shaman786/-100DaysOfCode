#include<iostream>
using namespace std;/* using- you are going to use it 
                       to use what? A namespace(A namespace is a declarative region that provides a scope to the identifiers (the names of types, functions, variables, etc) inside it. Namespaces are used to organize code into logical groups and to prevent name collisions that can occur especially when your code base includes multiple libraries)
                       std: The std namespace(where features of the c++ Standard Library, such as string or vector, are declared) */
int main()
{
    cout<<"Hello World."<<endl;// cout<< to write output to the screen
    //endl: end of line('\n')
    // (;)semicolon: to end a statement

    
}
